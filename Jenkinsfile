pipeline {
    agent any {
    stages {
          
          stage('Cloning from SCM'){
            steps{
            
              git 'https://github.com/manasapala/demo-java.git'
              }
          stage('Build & Package'){
            steps{
                
                sh 'mvn clean package'
                }
               }
           stage('
         }
   }
   }
